# XNO-connect integration test configuration
# Copy this file to .env and fill in your values

# RPC server URL
NANO_RPC_URL=https://rpc.nano.to

# What can be tested against just started local node,
# will be tested, not full ledger sync is needed.
LOCAL_NANO_RPC_URL=http://localhost:7076

# Your RPC key, tested with rpc.nano.to
# Used only to work generation
NANO_RPC_KEY=RPC-KEY-...

# WS server URL
NANO_WS_URL=wss://node.somenano.com/websocket

# The following variables are used in tests,
# there will be a 1 raw transaction from seed account
# to destination account with change of representative

# Seed (64 hex chars) for the wallet
NANO_SEED=your_64_char_hex_seed_here

# Destination account address (nano_xxx format)
NANO_DESTINATION=nano_destination_address_here

# New representative address (nano_xxx format)
NANO_REPRESENTATIVE=nano_representative_address_here
